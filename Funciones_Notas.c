#include <stdio.h>
#include <stdlib.h>
int suma(int a, int b, int RR){
RR= a+b;
return 0;}
int main()
{
    int x=10, y=20, res=0;

    suma(x, y, res);
    printf("%d", res);
    return 0;
}
